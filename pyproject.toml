[project]
name = "changelog"
version = "0.1.0"
description = "Utility repository for archived build metadata and generated project resources."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
